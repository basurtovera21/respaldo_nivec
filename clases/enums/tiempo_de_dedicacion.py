#Docente
from enum import Enum


class TiempoDeDedicacion(Enum):
    TIEMPO_COMPLETO = "Tiempo completo" #Normalmente 40 horas
    MEDIO_TIEMPO = "Medio tiempo" #Normalmente 20 horas
    TIEMPO_PARCIAL = "Tiempo parcial" #Horas específicas